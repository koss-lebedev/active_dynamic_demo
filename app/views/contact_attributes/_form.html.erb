<%= form_for(contact_attribute) do |f| %>
  <% if contact_attribute.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contact_attribute.errors.count, "error") %> prohibited this contact_attribute from being saved:</h2>

      <ul>
      <% contact_attribute.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :datatype %>
    <%= f.select :datatype, datatypes_for_select %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
